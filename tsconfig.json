{
  "compilerOptions": {
    "target": "ESNext", // Admite las últimas características de JavaScript.
    "lib": [
      "dom",
      "dom.iterable",
      "esnext"
    ], // Soporte para navegadores modernos y las API más recientes.
    "allowJs": true, // Permite archivos .js junto con .ts.
    "skipLibCheck": true, // Omite la verificación de tipos en bibliotecas para mejorar la velocidad de compilación.
    "strict": true, // Habilita estrictas verificaciones de tipo (muy recomendado).
    "forceConsistentCasingInFileNames": true, // Previene errores por diferencias en mayúsculas y minúsculas en nombres de archivo.
    "noEmit": true, // No genera archivos .js, ya que Next.js maneja el build.
    "esModuleInterop": true, // Mejora la interoperabilidad con módulos CommonJS y ESModule.
    "module": "ESNext", // Usa módulos modernos.
    "moduleResolution": "node", // Resolución de módulos compatible con Node.js.
    "resolveJsonModule": true, // Permite importar archivos JSON.
    "isolatedModules": true, // Necesario para el uso de TypeScript con Next.js.
    "jsx": "preserve", // Mantiene el JSX para que lo procese Next.js.
    "incremental": true, // Habilita compilaciones incrementales para mejorar el rendimiento.
    "baseUrl": ".", // Establece la raíz del proyecto.
    "paths": {
      "@/*": [
        "./src/*"
      ] // Alias para rutas absolutas.
    },
    "plugins": [
      {
        "name": "next"
      }
    ]
  },
  "include": [
    "next-env.d.ts", // Tipos generados automáticamente por Next.js.
    "**/*.ts", // Incluye todos los archivos .ts.
    "**/*.tsx", // Incluye todos los archivos .tsx.
    ".next/types/**/*.ts" // Incluye tipos generados por Next.js.
  ],
  "exclude": [
    "node_modules", // Excluye los módulos de Node.js.
    "**/*.test.ts", // Opcional: excluye archivos de prueba.
    "**/*.spec.ts", // Opcional: excluye archivos de pruebas unitarias.
    "**/__tests__/**" // Opcional: excluye carpetas específicas para pruebas.
  ]
}
